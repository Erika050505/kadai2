<% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>
    </div>
  <% end %>
   <div class="field">
      <%= form.label :title %>
      <%= form.text_field :title %>
     <% if product.errors.include?(:title) %>
       <p style="color: red;"><%= product.errors.full_messages_for(:title).first %>
     <% end %>
    </div>

    <div class="field">
      <%= form.label :body %>
      <%= form.number_field :body %>
     <% if product.errors.include?(:body) %>
       <p style="color: red;"><%= product.errors.full_messages_for(:body).first %>
     <% end %>
    </div>

    <div class="actions">
      <%= form.submit %>
    </div>
  <% end %>